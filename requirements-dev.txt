# Test- and lint-only dependencies. Runtime dependencies live in
# server/requirements.txt, which CI installs as well: prov.transport imports
# pywinrm at module level and vm/run-prov-test.py imports paramiko, and without
# them pytest dies during COLLECTION (INTERNALERROR) rather than failing a test,
# so the run reports nothing at all.
pytest
# Pinned so every machine and CI format and lint identically (rules in ruff.toml).
# The pin covers the default rule set too, not just formatter output -- 0.16.0
# enables substantially more by default than 0.15.x.
ruff==0.16.0
# Type checker (rules in pyrightconfig.json; the choice is argued in #87).
# basedpyright is pyright plus a stricter default rule set, packaged as a wheel
# that bundles node -- so it pins and installs like any other dep, with no npm
# fetch. Pinned for the same reason ruff is: the version decides what counts as a
# finding. Keep it in step with the Pylance in the editor, which is pyright 1.1.x.
basedpyright==1.39.10
