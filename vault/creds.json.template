{
    "_comment": "Copy this file to creds.json and fill in real values. creds.json is gitignored.",
    "_comment_unit": "Credentials of the local 'mast' admin account on the unit VM/machine.",
    "unit": {
        "user": ".\\mast",
        "pass": "physics"
    },
    "_comment_smb": "Read-only account on the provisioning server; units authenticate as this to pull staging payloads via SMB.",
    "smb": {
        "user": "mast-transfer",
        "pass": "CHANGE ME"
    }
}
