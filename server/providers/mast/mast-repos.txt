# One repo per line: host and path only (no https://, no .git suffix).
# Optionally append a ref (branch, tag, or commit hash) separated by whitespace.
# If no ref is given the remote default branch is cloned.
# Examples:
#   github.com/ORG/REPO
#   github.com/ORG/REPO main
#   github.com/ORG/REPO v1.2.3
#   github.com/ORG/REPO abc1234
# Lines starting with # and blank lines are ignored.
#
# Always give the ref explicitly. The repos do not share one integration branch
# (MAST_unit works on main, MAST_common on master), and an omitted ref silently
# follows whatever the remote default happens to be at clone time - which has
# already drifted from the working branch once.
github.com/The-MAST-project/MAST_unit.2024-12-12 main
github.com/The-MAST-project/MAST_common master
