{
  "name": "npcap",
  "version": "1.88",
  "description": "Install Npcap packet capture driver (WinPcap-compat, loopback, non-admin). Matches mastw, which also runs npcapwatchdog scheduled task to keep the npcap service alive. Wireshark and any other capture-based tooling depend on this.",
  "order": 1000,
  "command": "powershell.exe -ExecutionPolicy Bypass -NoProfile -NonInteractive -File \".\\provide-npcap.ps1\" -AssetsRoot \".\"",
  "commandfiles": [
    "provide-npcap.ps1",
    "verify-npcap.ps1",
    "assets/npcap-1.88.exe"
  ],
  "verify": "powershell.exe -ExecutionPolicy Bypass -NoProfile -NonInteractive -File \".\\verify-npcap.ps1\""
}

