{
  "name": "mongodb-client",
  "version": "mongosh-2.2.6/tools-100.9.4",
  "description": "Install MongoDB client tools: mongosh, Database Tools, and Compass (GUI). Does NOT install or start mongod. Compass keeps its own Squirrel updater, so a unit drifts off the pinned installer on its own; verify-mongodb-client.ps1 reports the build actually running rather than failing the unit for it -- see #137.",
  "order": 900,
  "command": "powershell.exe -ExecutionPolicy Bypass -NoProfile -NonInteractive -File \".\\provide-mongodb-client.ps1\" -AssetsRoot \".\" -InstallRoot \"C:\\Program Files\\MongoDB\"",
  "commandfiles": [
    "provide-mongodb-client.ps1",
    "compass-app.ps1",
    "verify-mongodb-client.ps1",
    "assets/mongosh-2.2.6-win32-x64.zip",
    "assets/mongodb-database-tools-windows-x86_64-100.9.4.zip",
    "assets/mongodb-compass-1.43.0-win32-x64.exe"
  ],
  "verify": "powershell.exe -ExecutionPolicy Bypass -NoProfile -NonInteractive -File \".\\verify-mongodb-client.ps1\" -InstallRoot \"C:\\Program Files\\MongoDB\""
}
